<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.kevin.inventorysupport">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <activity android:name=".main.MainActivity">
        </activity>
        <activity android:name=".main.SearchFormGBJ"></activity>
        <activity android:name=".main.DateSelect"></activity>
        <activity android:name=".main.ViewListGBJ"></activity>
        <activity android:name=".main.SearchFormGBB"></activity>
        <activity android:name=".main.ViewListGBB"></activity>
        <activity android:name=".main.ItemDetail"></activity>
        <activity android:name=".main.OrderDetail"></activity>

    </application>

</manifest>